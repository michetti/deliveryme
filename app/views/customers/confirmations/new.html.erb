<% content_for :head do %>
  <%= stylesheet_link_tag "devise", media: "all", "data-turbolinks-track" => true %>
<% end %>

<%= form_for resource,
             as: resource_name,
             url: confirmation_path(resource_name),
             html: { class: "form-devise" } do |f| %>

  <h2 class="form-devise-heading">Resend instructions</h2>

  <%= devise_error_messages! %>

  <%= f.email_field :email,
                    require: true,
                    autofocus: true,
                    placeholder: "Email address",
                    class: "form-control" %>

  <%= f.submit "Resend Instructions", class: "btn btn-lg btn-primary btn-block" %>
<% end %>

<%= render "/users/shared/links" %>
